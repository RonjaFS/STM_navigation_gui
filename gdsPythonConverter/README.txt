as now it works for Windows in the following way:

- check that all paths in imageToGds.py are with '/' instead of '\' (the other way around for Linux I think)

- use the terminal in visual Studio code and copy the Export part from Godot into the terminal

- type 'py ' in front of the code and Change Navigation\ Helper to Navigation_Helper (possibly the Image file Needs also to be copied in this Folder)

- Enter!